{
  "schemaVersion": 1,
  "debug": {
    "purpose": "CI verification artifact only; never a production identity.",
    "requireV2": true,
    "requireV3": false,
    "expectedCertificateSha256": null
  },
  "release": {
    "purpose": "Production/release identity.",
    "requiredSecrets": [
      "SIGNING_KEY",
      "KEY_STORE_PASSWORD",
      "KEY_ALIAS",
      "KEY_PASSWORD"
    ],
    "requireV2": true,
    "requireV3": true,
    "expectedCertificateSha256": "290fb37d527935766e327781833493400dd647cfc8bdbe433254a2df52e4b8e4",
    "statusUntilFingerprintIsPinned": "PINNED_PENDING_CI_PROOF"
  },
  "rules": [
    "Private key material must never be committed to the repository.",
    "Release signature validity is insufficient without a pinned expected certificate SHA-256 identity.",
    "Any certificate rotation requires an explicit lineage/update of this contract and invalidates affected release proof.",
    "The exact APK verified here must be the exact APK installed, tested, attested and distributed."
  ]
}
