name: ToolBox Public Tahap 5 Package

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'build.gradle'
      - 'settings.gradle'
      - '.github/workflows/public-app-package.yml'
      - 'AGENTS.md'
      - 'TASK.md'
      - 'R1_R9_ASSET_CHAIN.md'
      - 'ASSET_SAFE_100_RULES.md'
      - 'ASSET_SAFE_100_PROCESS.md'
      - 'ASSET_SAFE_100_METHODS.md'
      - 'ASSET_ROUTE_PROOF_PROCESS.md'
      - 'ASSET_ROUTE_PROOF_METHODS.md'
      - 'PREBUILD_ASSET_GATE.md'
      - 'APPLICATION_SAFE_100_PROCESS.md'
      - 'APP_SAFE_R*.md'
      - 'TEST_ROUTING_POLICY.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: public-tahap5-package-${{ github.ref }}
  cancel-in-progress: true

jobs:
  public-tahap5-package:
    runs-on: ubuntu-24.04
    timeout-minutes: 55

    steps:
      - name: Checkout exact public source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false

      - name: Set up exact Temurin JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961
        with:
          distribution: temurin
          java-version: '17.0.20+1'
          java-package: jdk
          architecture: x64
          check-latest: false

      - name: Set up Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699
        with:
          cmdline-tools-version: '14742923'
          packages: ''
          log-accepted-android-sdk-licenses: 'false'

      - name: Install Android API30 build and emulator inputs
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager 'platform-tools' 'emulator' 'platforms;android-30' 'build-tools;34.0.0' 'system-images;android-30;google_apis;x86_64'
          test -s "$ANDROID_SDK_ROOT/platforms/android-30/android.jar"
          test -x "$ANDROID_SDK_ROOT/build-tools/34.0.0/aapt"
          test -s "$ANDROID_SDK_ROOT/system-images/android-30/google_apis/x86_64/source.properties"

      - name: Set up exact Gradle without shared cache
        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
        with:
          gradle-version: '8.2.1'
          cache-disabled: true

      - name: Baseline Tahap 4 compatibility and Tahap 5 route gate
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,re
          from pathlib import Path
          gradle=Path('app/build.gradle').read_text()
          manifest=Path('app/src/main/AndroidManifest.xml').read_text()
          plan=json.loads(Path('app/ASSURANCE_PLAN_R1_R9.json').read_text())
          assert plan['stage']=='Tahap 5'
          assert plan['stageMap']=='E'
          assert plan['parentBaseline']['name']=='Tahap 4'
          assert plan['parentBaseline']['apkSha256']=='74a90ae0442083d4f050a4642a9c2a213ff1b785ea5b2f844f033cdea543cfc3'
          assert "applicationId 'com.toolbox.tools'" in gradle
          assert re.search(r'\bminSdk\s+30\b',gradle)
          assert re.search(r'\btargetSdk\s+30\b',gradle)
          assert re.search(r'\bversionCode\s+5\b',gradle)
          assert 'android:name=".MainActivity"' in manifest

          required=[
            'app/src/main/java/com/toolbox/tools/editor/BubblePositionStore.java',
            'app/src/main/java/com/toolbox/tools/editor/BubbleController.java',
            'app/src/main/java/com/toolbox/tools/editor/EdgePanelFactory.java',
            'app/src/main/java/com/toolbox/tools/editor/EditorShellController.java',
            'app/src/main/java/com/toolbox/tools/editor/FloatingEditorController.java',
            'app/src/main/java/com/toolbox/tools/editor/FloatingPlacementEngine.java',
            'app/src/main/java/com/toolbox/tools/editor/VisualEditorSession.java',
            'app/src/main/java/com/toolbox/tools/editor/VisualHistory.java',
            'app/src/main/java/com/toolbox/tools/editor/VisualLockSet.java',
            'app/src/main/java/com/toolbox/tools/editor/DefaultEditorFactory.java',
            'app/src/test/java/com/toolbox/tools/editor/BubbleShellTest.java',
            'app/src/test/java/com/toolbox/tools/editor/EdgeFloatingTest.java',
            'app/src/test/java/com/toolbox/tools/editor/VisualEditorSessionTest.java',
          ]
          for item in required:
            assert Path(item).is_file(),item
          print('BASELINE_TAHAP_4_COMPATIBILITY = PASS')
          print('TAHAP_5_E1_E4_ROUTE = PASS')
          PY

      - name: Automatic R1-R8 prebuild assurance
        run: python3 app/scripts/assurance_prebuild.py

      - name: Automatic ASSET_SAFE regression prebuild
        run: python3 app/scripts/asset_validate.py

      - name: Run Tahap 5 unit and failure tests
        run: gradle --no-daemon :app:testDebugUnitTest

      - name: Automatic R9 mutation challenge
        run: python3 app/scripts/mutation_test.py

      - name: Restore clean full test evidence after mutation
        run: gradle --no-daemon :app:testDebugUnitTest --rerun-tasks

      - name: Assemble Tahap 5 release and debug packages
        run: |
          gradle --no-daemon :app:assembleRelease
          gradle --no-daemon :app:assembleDebug

      - name: Verify Tahap 5 package metadata and digest
        shell: bash
        run: |
          set -euo pipefail
          APK="app/build/outputs/apk/release/app-release-unsigned.apk"
          DEBUG_APK="app/build/outputs/apk/debug/app-debug.apk"
          test -s "$APK"
          test -s "$DEBUG_APK"
          BADGING="$("$ANDROID_SDK_ROOT/build-tools/34.0.0/aapt" dump badging "$APK")"
          grep -F "package: name='com.toolbox.tools'" <<< "$BADGING"
          grep -F "versionCode='5'" <<< "$BADGING"
          grep -F "sdkVersion:'30'" <<< "$BADGING"
          grep -F "targetSdkVersion:'30'" <<< "$BADGING"
          sha256sum "$APK" | tee "$APK.sha256"
          echo "TAHAP_5_PUBLIC_UNSIGNED_APK = PASS"

      - name: Boot API30 emulator and interact with Bubble Edge and Floating Editor
        shell: bash
        run: |
          set -euo pipefail
          sudo chmod 666 /dev/kvm || true
          export ANDROID_AVD_HOME="$RUNNER_TEMP/toolbox-tahap5-avd-home"
          mkdir -p "$ANDROID_AVD_HOME"
          AVD_PATH="$ANDROID_AVD_HOME/tahap5-api30.avd"
          printf '\n' | avdmanager create avd --force --name tahap5-api30 --package 'system-images;android-30;google_apis;x86_64' --path "$AVD_PATH" --device 'pixel_2'
          "$ANDROID_SDK_ROOT/emulator/emulator" -avd tahap5-api30 -no-window -no-audio -no-boot-anim -no-snapshot -wipe-data -gpu swiftshader_indirect > "$RUNNER_TEMP/tahap5-emulator.log" 2>&1 &
          EMULATOR_PID=$!
          trap 'kill "$EMULATOR_PID" 2>/dev/null || true' EXIT

          timeout 120s adb wait-for-device
          booted=0
          for _ in $(seq 1 90); do
            state="$(timeout 10s adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r' || true)"
            if [[ "$state" == "1" ]]; then booted=1; break; fi
            kill -0 "$EMULATOR_PID" 2>/dev/null || {
              tail -n 200 "$RUNNER_TEMP/tahap5-emulator.log" >&2 || true
              exit 1
            }
            sleep 2
          done
          test "$booted" = "1"

          API_LEVEL="$(adb shell getprop ro.build.version.sdk | tr -d '\r')"
          ABI="$(adb shell getprop ro.product.cpu.abi | tr -d '\r')"
          test "$API_LEVEL" = "30"
          test "$ABI" = "x86_64"

          adb install -r app/build/outputs/apk/debug/app-debug.apk
          START="$(adb shell am start -W -n com.toolbox.tools/.MainActivity)"
          printf '%s\n' "$START"
          grep -F 'Status: ok' <<< "$START"
          sleep 2

          dump_ui() {
            adb shell uiautomator dump /sdcard/toolbox-window.xml >/dev/null
            adb pull /sdcard/toolbox-window.xml "$RUNNER_TEMP/toolbox-window.xml" >/dev/null
          }

          tap_text() {
            local target="$1"
            dump_ui
            read X Y < <(python3 - "$RUNNER_TEMP/toolbox-window.xml" "$target" <<'PY'
          import re,sys,xml.etree.ElementTree as ET
          root=ET.parse(sys.argv[1]).getroot()
          target=sys.argv[2]
          for node in root.iter('node'):
              if node.attrib.get('text')==target:
                  m=re.fullmatch(r'\[(\d+),(\d+)\]\[(\d+),(\d+)\]',node.attrib['bounds'])
                  if m:
                      l,t,r,b=map(int,m.groups())
                      print((l+r)//2,(t+b)//2)
                      raise SystemExit(0)
          raise SystemExit("UI_TEXT_NOT_FOUND="+target)
          PY
            )
            adb shell input tap "$X" "$Y"
            sleep 1
          }

          dump_ui
          WINDOW="$(cat "$RUNNER_TEMP/toolbox-window.xml")"
          grep -F 'ToolBox Tahap 5' <<< "$WINDOW"
          grep -F 'Visual Editor' <<< "$WINDOW"
          grep -F 'TB' <<< "$WINDOW"

          tap_text 'TB'
          dump_ui
          WINDOW="$(cat "$RUNNER_TEMP/toolbox-window.xml")"
          grep -F 'Tambah ke Layar' <<< "$WINDOW"
          grep -F 'Komponen' <<< "$WINDOW"

          tap_text 'Buka Detail'
          dump_ui
          WINDOW="$(cat "$RUNNER_TEMP/toolbox-window.xml")"
          grep -F 'Edit Object' <<< "$WINDOW"
          grep -F 'Size' <<< "$WINDOW"
          grep -F 'Auto Connect Binding' <<< "$WINDOW"

          tap_text 'Size'
          dump_ui
          WINDOW="$(cat "$RUNNER_TEMP/toolbox-window.xml")"
          grep -F 'Floating Editor • Size' <<< "$WINDOW"
          grep -F 'X Tutup' <<< "$WINDOW"

          adb exec-out screencap -p > app/build/assurance/tahap5-api30.png
          {
            echo "API_LEVEL=$API_LEVEL"
            echo "EMULATOR_ABI=$ABI"
            echo "API30_APP_LAUNCH=PASS"
            echo "TAHAP5_UI_TEXT=PASS"
            echo "BUBBLE_INTERACTION=PASS"
            echo "EDGE_CONTEXT=PASS"
            echo "FLOATING_EDITOR_INTERACTION=PASS"
            echo "APP_THEME_RUNTIME=PASS"
          } > app/build/assurance/tahap5-api30-runtime.txt

      - name: Finalize ASSET_SAFE package/runtime proof
        run: python3 app/scripts/asset_finalize.py

      - name: Automatic R9 completeness and provenance closure
        run: python3 app/scripts/assurance_finalize.py

      - name: Upload Tahap 5 unsigned package and assurance
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4
        with:
          name: toolbox-tahap5-public-unsigned-ready-private-signing
          path: |
            app/build/outputs/apk/release/app-release-unsigned.apk
            app/build/outputs/apk/release/app-release-unsigned.apk.sha256
            app/build/test-results/testDebugUnitTest/
            app/build/assurance/tahap5-r1-r8-prebuild-evidence.json
            app/build/assurance/tahap5-asset-prebuild-evidence.json
            app/build/assurance/tahap5-asset-final-evidence.json
            app/build/assurance/tahap5-mutation-evidence.json
            app/build/assurance/tahap5-r1-r9-evidence.json
            app/build/assurance/tahap5-api30-runtime.txt
            app/build/assurance/tahap5-api30.png
          if-no-files-found: error
          retention-days: 7
