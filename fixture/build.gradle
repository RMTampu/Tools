plugins {
    id 'com.android.application'
}

android {
    namespace 'com.toolbox.fixture'
    compileSdk 30

    defaultConfig {
        applicationId 'com.toolbox.fixture'
        minSdk 30
        targetSdk 30
        versionCode 1
        versionName '1.0-test-fixture'
    }

    lint {
        disable 'ExpiredTargetSdkVersion'
    }
}
